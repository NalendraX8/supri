{
  "name": "supri-api",
  "version": "1.0.0",
  "description": "Mock API server for Supri POS",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "start:remote": "json-server db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
